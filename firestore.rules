rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permit broad access to all collections to align with the application's custom authentication system
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
